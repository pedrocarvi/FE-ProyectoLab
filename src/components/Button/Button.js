// children palabra reservada de react que viene siempre
// children recibe lo que pongamos entre las etiquetas

// const Button = ({ children }) => {
//   return <button>{children}</button>;
// };

// export default Button;
